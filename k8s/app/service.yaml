apiVersion: v1
kind: Service
metadata:
  name: monitoring-app
  labels:
    app: monitoring-app
spec:
  type: ClusterIP
  selector:
    app: monitoring-app
  ports:
    - name: http
      port: 3001
      targetPort: 3001
